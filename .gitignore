# Raw IBTrACS data — build inputs only. Too big for GitHub (100 MB/file limit) and not
# needed on the web (the site serves data/, generated by process_storms.py).
*.csv
*.csv.tmp
*.tmp

# macOS / editor junk
.DS_Store
**/.DS_Store
Icon?
._*
